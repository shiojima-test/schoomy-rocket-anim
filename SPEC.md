# ペットボトルロケット 飛行データ 仕様

株式会社スクーミー ／ 2026年8月6日 実測

## このリポジトリの内容

| ファイル | 内容 |
|---|---|
| `data/FLT008_flight.csv` | 飛行区間だけを抜き出したもの（469行 / 4.68秒）。**通常はこちらを使用** |
| `data/FLT008_full.csv` | 記録の全体（3433行 / 36.1秒） |
| `SPEC.md` | このファイル |

## CSVの形式

```
# rocket_logger v3.6 rate=100Hz accel=16G(2048/G) gyro=2000dps(16.4/dps) sound=A1 boot=6
t_ms,ax,ay,az,gx,gy,gz,snd_min,snd_max
35078,-1449,-8,1602,-6,10,-46,507,507
```

1行目は設定メモ（`#` で始まる）。2行目が列名。3行目以降が測定値です。

| 列 | 内容 | 換算 |
|---|---|---|
| `t_ms` | 電源投入からの経過時間 | ÷1000 で秒 |
| `ax` `ay` `az` | 加速度3軸の生値 | ÷2048 で G |
| `gx` `gy` `gz` | 角速度3軸の生値 | ÷16.4 で deg/s |
| `snd_min` `snd_max` | その10msの音の最小・最大 | `snd_max − snd_min` が音の大きさ |

記録周期は100Hz（10ms間隔）。値はセンサーの生値で、換算はしていません。

## 確定している時刻

再計算は不要です。この値を使用してください。

| | 時刻 | 発射から |
|---|---|---|
| t0 発射 | 35.58 秒 | 0.00 秒 |
| t1 噴射終了 | 35.98 秒 | 0.41 秒 |
| 接地 1回目 | 38.64 秒 | 3.06 秒 |
| 接地 2回目 | 39.14 秒 | 3.56 秒 |
| 接地 3回目 | 39.30 秒 | 3.72 秒 |
| t2 完全静止 | 39.46 秒 | 3.89 秒 |

**着地では機体が跳ね返り、3回地面に当たっています。**
滞空時間は「発射してから完全に止まるまで」として 3.89 秒です。

## 確定している測定値

| 項目 | 値 | 備考 |
|---|---|---|
| 滞空時間 | 3.89 秒 | 完全に止まるまで |
| 噴射時間 | 0.41 秒 | |
| 打上げ加速度 | 17.0 G | 測定上限16Gで飽和。実際はこれ以上 |
| 着地衝撃 | 22.8 G | 同上 |
| 接地1回目の衝撃 | 19.6 G | |
| 接地2回目の衝撃 | 22.8 G | |
| 接地3回目の衝撃 | 17.8 G | |
| 回転（合成）平均 | 26.3 rpm | 噴射終了〜静止 |
| 回転（合成）最大 | 303 rpm | |
| 　ロール | 14.5 rpm | 機体の軸まわり |
| 　ピッチ | 12.9 rpm | 機首が上下に振れる |
| 　ヨー | 14.0 rpm | 機首が左右に振れる |
| 飛行中に何回転したか | 1.5 回転 | |
| 揺れ | 26.66 m/s² | 合成加速度の標準偏差 |
| 騒音 | 723 | 音の振れ幅の最大 − 発射前 |

## 姿勢の復元に使う値

発射前の静止区間から求めた**重力の向き**（機体座標系の単位ベクトル）

```
X -0.6405   Y 0.0046   Z 0.7679
```

基板のZ軸は鉛直から約 40 度傾いています。発射台に斜めに設置されていたためです。

噴射中の加速度から求めた**機体の3軸**（互いに直交）

| 軸 | X | Y | Z | 意味 |
|---|---|---|---|---|
| ロール | -0.9450 | -0.2953 | 0.1407 | 機体の長軸 |
| ピッチ | 0.0459 | 0.3060 | 0.9509 | ヨー軸 × ロール軸 |
| ヨー | -0.3238 | 0.9051 | -0.2756 | ロール軸 × 重力方向 |

## 再現できるもの・できないもの

| | 可否 | 根拠 |
|---|---|---|
| 機体の姿勢（回転） | **再現可能** | 角速度3軸を100Hzで記録済み |
| 発射・噴射終了・着地の時刻 | **実測** | 加速度から判定済み |
| 機体にかかった力・音 | **実測** | |
| 飛行の軌道（位置） | **再現不可** | 気圧センサーもGPSもない |
| 到達高度 | **再現不可** | 同上 |

加速度を二重積分すると変位79mという非現実的な値になります。
機体が回転するため、機体座標系の加速度をそのまま積分しても軌道にはなりません。

**軌道はモデルで補い、実測でないことを明示してください。**

## 測定の限界

- 打上げ加速度・着地衝撃とも測定上限16Gに達しており、真の値は不明です
- 着地の衝撃は数ミリ秒で終わるため、100Hzでは1点しか記録されません
- 音センサーは噴射音でもポンプ音でも上限に達しており、機体による差が出ません

## 色の指定

既存資料と揃えるための色です。

```
文字色  #1F2937
青     #1F6FB2   ロール
橙     #D97706   ピッチ
緑     #1F8A54   ヨー
背景   #FFFFFF   グラフ背景 #FBFCFC   罫線 #E5E7EB
```
